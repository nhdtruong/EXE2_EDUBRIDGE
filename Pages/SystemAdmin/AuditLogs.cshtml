@page
@model EduBridge.Pages.SystemAdmin.AuditLogsModel
@{
ViewData["Title"] = "Nhật ký Hệ thống";
Layout = "_AdminLayout";
}

<div class="space-y-6 pb-20">
    <div>
        <h1 class="text-3xl font-bold text-gray-900">Nhật ký Hệ thống</h1>
        <p class="text-gray-500 mt-1">Lịch sử hoạt động của các quản trị viên.</p>
    </div>

    <div class="rounded-2xl border border-gray-200 bg-white shadow-sm overflow-hidden">
        <div class="border-b border-blue-100 bg-blue-50/60 p-6">
            <h3 class="text-base font-bold text-gray-900">Bộ lọc tìm kiếm</h3>

            <form id="filterForm" method="get" class="mt-5 space-y-4">
                <input type="hidden" asp-for="PageNumber" id="PageNumber" />
                <div class="grid gap-4 lg:grid-cols-2">
                    <div>
                        <label class="sr-only">Tìm kiếm</label>
                        <input type="text" asp-for="Search" class="block h-12 w-full rounded border border-gray-300 bg-white px-4 text-sm text-gray-900 placeholder-gray-500 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500" placeholder="Tìm kiếm hành động, đối tượng, người thực hiện..." />
                    </div>

                    <div>
                        <label class="sr-only">Khoảng ngày</label>
                        <div class="ds-datepicker" data-ds-datepicker data-ds-range="true">
                            <input type="hidden" name="FilterDateRange" value="@Model.FilterDateRange" data-ds-date-value>
                            <div class="ds-datepicker-field flex h-12 w-full items-center overflow-hidden rounded border border-gray-300 bg-white focus-within:border-blue-500 focus-within:ring-1 focus-within:ring-blue-500">
                                <input class="ds-datepicker-input h-full w-full border-none bg-transparent px-4 text-sm text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-0" type="text" placeholder="Khoảng ngày (dd/mm/yyyy)" data-ds-date-display>
                                <button type="button" class="ds-datepicker-button flex h-full w-12 items-center justify-center bg-blue-600 text-white hover:bg-blue-700 transition-colors" data-ds-date-toggle aria-label="Chọn ngày">
                                    <svg width="18" height="18" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3M4 11h16M5 5h14a1 1 0 011 1v14a1 1 0 01-1 1H5a1 1 0 01-1-1V6a1 1 0 011-1z"></path></svg>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="flex items-center justify-end gap-3">
                    <a asp-page="/SystemAdmin/AuditLogs" class="inline-flex h-11 items-center rounded bg-slate-600 px-4 text-sm font-semibold text-white shadow-sm transition-colors hover:bg-slate-700">
                        Làm mới bộ lọc
                    </a>
                    <button type="submit" class="inline-flex h-11 items-center rounded bg-blue-600 px-4 text-sm font-semibold text-white shadow-sm transition-colors hover:bg-blue-700">
                        <svg class="mr-2 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                        Tìm kiếm
                    </button>
                </div>

            </form>
        </div>

        <div class="overflow-x-auto">
            <table class="w-full text-left border-collapse">
                <thead>
                    <tr class="whitespace-nowrap bg-slate-100 border-b border-slate-200 text-sm font-bold text-slate-800">
                        <th class="py-4 px-6 font-bold border-r border-slate-200">Thời gian</th>
                        <th class="py-4 px-6 font-bold border-r border-slate-200">Hành động</th>
                        <th class="py-4 px-6 font-bold border-r border-slate-200">Người thực hiện</th>
                        <th class="py-4 px-6 font-bold border-r border-slate-200">Đối tượng</th>
                        <th class="py-4 px-6 font-bold border-r border-slate-200">Trung tâm</th>
                        <th class="py-4 px-6 font-bold">Project</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-100">
                    @foreach (var log in Model.Logs)
                    {
                        <tr class="hover:bg-slate-50 transition-colors">
                            <td class="py-4 px-6 text-sm text-slate-600 border-r border-slate-100">@log.CreatedAt.ToString("dd/MM/yyyy HH:mm")</td>
                            <td class="py-4 px-6 text-sm font-semibold border-r border-slate-100">
                                <span class="bg-gray-100 text-gray-800 px-2 py-1 rounded border border-gray-200 text-xs">@log.Action</span>
                            </td>
                            <td class="py-4 px-6 text-sm text-slate-900 border-r border-slate-100">@(log.ActorUser?.FullName ?? $"User {log.ActorUserId}")</td>
                            <td class="py-4 px-6 text-sm text-slate-600 border-r border-slate-100">
                                @log.EntityName - <span class="font-mono text-xs">@log.EntityId</span>
                            </td>
                            <td class="py-4 px-6 text-sm text-slate-600 border-r border-slate-100">@(log.TargetCenter?.CenterName ?? "-")</td>
                            <td class="py-4 px-6 text-sm text-slate-600">@(log.TargetProject?.ProjectName ?? "-")</td>
                        </tr>
                    }
                    @if (Model.Logs.Count == 0)
                    {
                        <tr>
                            <td colspan="6" class="px-6 py-8 text-center text-gray-500">
                                Không tìm thấy nhật ký nào.
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="border-t border-slate-200 bg-white px-6 py-4">
            <div class="ds-pagination">
                <div class="ds-pagination-pages">
                    <a href="javascript:void(0)" onclick="setPageAndSubmit(1)" class="ds-page-link @(Model.PageNumber <= 1 ? "ds-page-link-disabled" : "")">«</a>
                    <a href="javascript:void(0)" onclick="setPageAndSubmit(@(Model.PageNumber - 1))" class="ds-page-link @(Model.PageNumber <= 1 ? "ds-page-link-disabled" : "")">‹</a>

                    @{
                    int startPage = Math.Max(1, Model.PageNumber - 2);
                    int endPage = Math.Min(Model.TotalPages, startPage + 4);
                    if (endPage - startPage < 4)
                    {
                        startPage = Math.Max(1, endPage - 4);
                    }
                }

                @for (int i = startPage; i <= endPage; i++)
                {
                    if (i == Model.PageNumber)
                    {
                        <a href="javascript:void(0)" class="ds-page-link ds-page-link-active">@i</a>
                    }
                    else
                    {
                        <a href="javascript:void(0)" onclick="setPageAndSubmit(@i)" class="ds-page-link">@i</a>
                    }
                }

                <a href="javascript:void(0)" onclick="setPageAndSubmit(@(Model.PageNumber + 1))" class="ds-page-link @(Model.PageNumber >= Model.TotalPages ? "ds-page-link-disabled" : "")">›</a>
                <a href="javascript:void(0)" onclick="setPageAndSubmit(@Model.TotalPages)" class="ds-page-link @(Model.PageNumber >= Model.TotalPages ? "ds-page-link-disabled" : "")">»</a>
            </div>
            <div class="ds-pagination-meta">
                <span>Tổng số: @Model.TotalItems bản ghi</span>
                <select class="ds-page-size" form="filterForm" name="PageSize" onchange="document.getElementById('PageNumber').value=1; this.form.submit()">
                    @foreach (var size in Model.PageSizeOptions)
                    {
                        if (size == Model.PageSize)
                        {
                            <option value="@size" selected>@size</option>
                        }
                        else
                        {
                            <option value="@size">@size</option>
                        }
                    }
                </select>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<link rel="stylesheet" href="~/css/design-system/datepicker.css" asp-append-version="true" />
<link rel="stylesheet" href="~/css/design-system/pagination.css" asp-append-version="true" />
<script src="~/js/design-system/datepicker.js" asp-append-version="true"></script>
<script>
    function setPageAndSubmit(pageNum) {
    document.getElementById('PageNumber').value = pageNum;
    document.getElementById('filterForm').submit();
}
</script>
}