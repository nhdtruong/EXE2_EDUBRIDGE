@using System.Security.Claims
@{
    var fullName = User.FindFirstValue(ClaimTypes.Name) ?? "Người dùng";
    var email = User.FindFirstValue(ClaimTypes.Email) ?? string.Empty;
    var role = User.FindFirstValue(ClaimTypes.Role) ?? string.Empty;
    var roleDisplay = role switch
    {
        "OWNER" => "Chủ trung tâm",
        "TEACHER" => "Giáo viên",
        "PARENT" => "Phụ huynh",
        _ => "Người dùng"
    };
    var initials = string.Join(
        string.Empty,
        fullName
            .Split(' ', StringSplitOptions.RemoveEmptyEntries)
            .TakeLast(2)
            .Select(part => part[0]))
        .ToUpperInvariant();

    if (string.IsNullOrWhiteSpace(initials))
    {
        initials = "U";
    }
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - EduBridge</title>
    <!-- Import Google Fonts API -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/teacher.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/teacher-premium.css" asp-append-version="true" />
</head>
<body class="bg-gray">
    <div id="layout-toast" style="position: fixed; right: 24px; top: 24px; z-index: 9999; display: none; width: 360px; border-radius: 12px; background: #16a34a; padding: 16px 20px; color: #fff; box-shadow: 0 20px 35px rgba(15, 23, 42, 0.22);">
        <div id="layout-toast-title" style="font-size: 18px; line-height: 24px; font-weight: 800;">Thành công</div>
        <div id="layout-toast-message" style="margin-top: 4px; font-size: 14px; line-height: 20px; font-weight: 600; color: rgba(255,255,255,0.95);"></div>
    </div>

    <div class="app-container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="sidebar-brand">
                <div class="logo-box">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <polygon points="12 2 2 7 12 12 22 7 12 2"></polygon>
                        <polyline points="2 17 12 22 22 17"></polyline>
                        <polyline points="2 12 12 17 22 12"></polyline>
                    </svg>
                </div>
                <div class="brand-text">
                    <span class="brand-name">EduBridge</span>
                    <span class="brand-sub">Giáo viên</span>
                </div>
            </div>

            <nav class="sidebar-nav">
                <a href="/Teacher/Dashboard" class="nav-item @(ViewData["ActivePage"] as string == "Dashboard" ? "active" : "")">
                    <svg class="nav-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="7" height="7"></rect><rect x="14" y="3" width="7" height="7"></rect><rect x="14" y="14" width="7" height="7"></rect><rect x="3" y="14" width="7" height="7"></rect></svg>
                    <span>Dashboard</span>
                </a>
                <a href="/Teacher/Lectures" class="nav-item @(ViewData["ActivePage"] as string == "Lectures" ? "active" : "")">
                    <svg class="nav-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"></path><path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"></path></svg>
                    <span>Bài giảng</span>
                </a>
                <a href="/Homework" class="nav-item @(ViewData["ActivePage"] as string == "Assignments" ? "active" : "")">
                    <svg class="nav-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path><polyline points="14 2 14 8 20 8"></polyline><line x1="16" y1="13" x2="8" y2="13"></line><line x1="16" y1="17" x2="8" y2="17"></line><polyline points="10 9 9 9 8 9"></polyline></svg>
                    <span>Bài tập</span>
                </a>
                <a href="/Grades" class="nav-item @(ViewData["ActivePage"] as string == "Grades" ? "active" : "")">
                    <svg class="nav-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9H4.5a2.5 2.5 0 0 1 0-5H6"></path><path d="M18 9h1.5a2.5 2.5 0 0 0 0-5H18"></path><path d="M4 22h16"></path><path d="M10 14.66V17c0 .55-.47.98-.97 1.21C7.85 18.75 7 20.24 7 22"></path><path d="M14 14.66V17c0 .55.47.98.97 1.21C16.15 18.75 17 20.24 17 22"></path><path d="M18 2H6v7a6 6 0 0 0 12 0V2Z"></path></svg>
                    <span>Sổ điểm</span>
                </a>
                <a href="/Teacher/Attendance" class="nav-item @(ViewData["ActivePage"] as string == "Attendance" ? "active" : "")">
                    <svg class="nav-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path><circle cx="12" cy="7" r="4"></circle></svg>
                    <span>Điểm danh</span>
                </a>
                <a href="/Messages" class="nav-item @(ViewData["ActivePage"] as string == "Messages" ? "active" : "")">
                    <svg class="nav-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg>
                    <span>Tin nhắn</span>
                </a>
            </nav>

            <div class="sidebar-footer">
                <div class="user-profile">
                    <div class="avatar">@initials</div>
                    <div class="user-info">
                        <span class="user-name">@fullName</span>
                        <span class="user-role">@roleDisplay</span>
                    </div>
                </div>
                <form method="post" asp-page="/Logout">
                    <button type="submit" class="logout-btn">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="me-2"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path><polyline points="16 17 21 12 16 7"></polyline><line x1="21" y1="12" x2="9" y2="12"></line></svg>
                        Đăng xuất
                    </button>
                </form>
            </div>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            @RenderBody()
        </main>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script>
        const layoutToastMessage = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(TempData["ToastMessage"] as string));

        if (layoutToastMessage) {
            const toast = document.getElementById('layout-toast');
            document.getElementById('layout-toast-title').textContent = 'Thành công';
            document.getElementById('layout-toast-message').textContent = layoutToastMessage;
            toast.style.display = 'block';

            setTimeout(() => {
                toast.style.display = 'none';
            }, 3000);
        }
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
